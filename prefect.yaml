deployments:
  - name: ssw-tracking-sync
    flow_name: ssw_tracking_sync
    entrypoint: main.py:ssw_tracking_sync
    schedule:
      cron: "*/60 * * * *"   # a cada 1 hora
      timezone: "America/Sao_Paulo"
    work_pool:
      name: default-pool
